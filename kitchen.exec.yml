---
driver:
  name: exec

transport:
  name: exec

provisioner:
  name: chef_zero
  enforce_idempotency: true
  multiple_converge: 2
  deprecations_as_errors: true

platforms:
  - name: windows-latest
